To use this software, just download the folders dist and build, then double click the executable

To rebuild, use the command pyinstaller labeller.py in anaconda prompt (after making your changes to labeller.py)